# MySQL, reached through the SSH tunnel on 127.0.0.1:3307.
# Use a SELECT-only user.
DB_URL=mysql+pymysql://user:password@127.0.0.1:3307/database

# Only needed if you switch off the local Ollama model.
OPENAI_API_KEY=
GEMINI_API_KEY=
